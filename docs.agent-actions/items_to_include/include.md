- we need to explain hierachy of configs
  The migration from old format to new format is now complete with full project-level configuration integration. The new format successfully implements the 3-level configuration hierarchy (project → workflow → action)
  