default_agent_config:
  api_key: OPENAI_API_KEY
  chunk_config:
    overlap: 500
    chunk_size: 4000
  model_name: gpt-4o-mini
  prompt_debug: False
schema_path: schema
tool_path: ["tools"]
output_storage:
  backend: sqlite
  db_path: ./agent_io/outputs.db
