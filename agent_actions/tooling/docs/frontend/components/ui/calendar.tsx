'use client'

// Calendar component placeholder — not currently used in the docs UI.
// To restore: install react-day-picker and date-fns, then re-add the
// shadcn/ui Calendar implementation.

export type CalendarProps = Record<string, never>
export function Calendar() {
  return null
}
