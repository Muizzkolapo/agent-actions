# Config Manifest

## Overview

Vendor configuration helpers used by both batch and realtime connectors.

## Modules

| Name | Type | Description | Signals |
|------|------|-------------|---------|
| `vendor.py` | Module | Defines vendor profiles + defaults (timeouts, engines). | `llm.providers` |
