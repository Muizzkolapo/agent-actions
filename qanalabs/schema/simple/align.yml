{
    "title": "Quiz",
    "description": "A quiz containing a list of questions.",
    "type": "object",
    "properties": {
        "questions": {
            "type": "array",
            "title": "Questions",
            "description": "An array of questions.",
            "items": {
                "type": "object",
                "properties": {
                    "scenario": {
                        "type": "string",
                        "title": "Scenario",
                        "description": "The technical scenario for the question (No question should be posed here, no question marks)",
                        "pattern": "^[^??]*$"  # This regex pattern disallows question marks
                    },
                    "question": {
                        "type": "string",
                        "title": "Question",
                        "description": "The Question based on the scenario",
                        "pattern": "^[^scenario]*$"  # This regex pattern disallows question marks
                    },
                    "options": {
                        "type": "array",
                        "title": "Options",
                        "description": "List of options",
                        "items": {
                            "type": "string",
                            "pattern": "^(?=.*\\b(explain|describe|elaborate|discuss|how|why|what)\\b)(?=.*\\b(because|since|as|thus|therefore)\\b)(?=.*[,;]).{300,}$"
                        },
                        "minItems": 4,
                        "maxItems": 5
                    },
                    "answer": {
                        "type": "string",
                        "title": "Answer",
                        "description": "Must include the correct answer to the question",
                        "enum": ["A", "B", "C", "D", "E"]
                    },
                    "explanation": {
                        "type": "string",
                        "title": "Explanation",
                        "description": "The explanation of the correct answer"
                    },
                    "align": {
                        "type": "boolean",
                        "title": "align",
                        "description": "Indicates if the question components align with the source data. True if they align, false otherwise"
                    }
                },
                "required": ["scenario", "question", "options", "answer", "explanation","align"]
            }
        }
    },
    "required": ["questions"]
}