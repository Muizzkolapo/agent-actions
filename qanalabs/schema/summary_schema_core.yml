title: "Summary"
description: "A list of key summaries"
type: "object"
properties:
  summaries:
    title: "Allsummaries"
    description: "List of summaries"
    type: "array"
    items:
      type: "string"
      pattern: "^(?=.*\\b(explain|describe|elaborate|discuss|how|why|what)\\b)(?=.*\\b(because|since|as|thus|therefore)\\b)(?=.*[,;]).{300,}$"