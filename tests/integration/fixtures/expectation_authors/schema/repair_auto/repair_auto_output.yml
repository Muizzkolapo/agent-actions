name: repair_auto_output
fields:
  - id: summary
    type: string
  - id: exam_density
    type: string
    expectations:
      - id: density_is_known
        type: accepted_values
        params: {values: [ONLY_THIS_TOKEN]}
        hint: "Answer with exactly high, medium or low."
additionalProperties: true
