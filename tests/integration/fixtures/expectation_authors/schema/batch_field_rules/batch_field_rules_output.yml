name: batch_field_rules_output
fields:
  - id: summary
    type: string
    expectations:
      - id: summary_present
        type: not_null
  - id: exam_density
    type: string
    expectations:
      - id: density_is_known
        type: accepted_values
        params: {values: [high, medium, low]}
additionalProperties: true
